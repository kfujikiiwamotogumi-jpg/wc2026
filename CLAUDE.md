# CLAUDE.md

このリポジトリで作業する AI アシスタント（および人間）向けのガイドです。

## プロジェクト概要

これは **FIFA ワールドカップ 2026** の試合日程（全104試合）を表示する
**単一ファイルの静的 Web アプリ**です。マークアップ・スタイル・データ・ロジックの
すべてが `index.html` の中に入っています。ビルド手順・パッケージマネージャー・
依存関係・バックエンドはありません。

UI は **日本語**で、モバイルファースト設計（iOS のセーフエリア対応、スティッキー
ヘッダー、ボトムナビゲーション）です。キックオフ時刻はすべて
**日本時間（JST）**に変換して表示します。

### 主な機能
- グループステージから決勝まで全104試合の日程。
- 横スクロールのチップ（`#teamRow`）によるチーム別フィルター。
- チーム名・会場でのフリーテキスト検索（`#searchInput`）。
- 「全試合」「日本代表」のクイックフィルターボタン（`#bottomNav`）。
- ステージごとにグループ化し、色付きのステージヘッダーを表示。
- 日本戦は視覚的に強調（赤いアクセント + 🇯🇵）。
- 特定チームの全試合を表示できるモーダル（`#modal`）。

## リポジトリ構成

```
.
└── index.html      # アプリ全体（HTML + CSS + JS が1ファイルに集約）
```

意図的にソースファイルは1つだけです。

## 実行 / 開発方法

**ビルドやインストールの手順はありません。**作業するには:

- `index.html` をブラウザで直接開く、または
- 任意の静的サーバーでこのディレクトリを配信する。例:
  ```bash
  python3 -m http.server 8000
  # その後 http://localhost:8000 を開く
  ```

変更を確認するにはページを再読み込みし、表示された日程・チームチップ・検索・
日本フィルター・モーダルを確認してください。自動テストや Linter は設定されて
いません。

## コード構成（`index.html` 内）

ファイルは3つのセクションで構成されます: `<style>`、HTML の body、`<script>`。

### データモデル
- **`RAW`** — 信頼できる唯一の情報源（source of truth）となる試合データ配列。
  各要素の形:
  ```js
  {home, away, group, stage, venue, city, etDate, etTime}
  ```
  - `etDate` / `etTime` は**東部時間（ET）**の文字列: `"YYYYMMDD"` と `"HHMM"`。
  - `group` はグループ文字（`"A"`〜`"L"`）、ノックアウト戦では `""`。
  - ノックアウト戦のプレースホルダーは `"2位A"`（グループAの2位）、
    `"1位C"`（グループCの1位）、`"R32勝者"`、`"QF勝者"`、`"決勝"` などのラベル。
- **`MATCHES`** — `RAW` から `.map()` で生成。`id` と JST 変換した
  `jDate`・`jTime`・`jDay` を追加します。**`MATCHES` を直接編集しないこと。
  必ず `RAW` を編集してください。**
- **`ALL_TEAMS`** — 実在するチーム名の重複なしソート済みリスト。プレースホルダー
  文字列（`位`・`勝者`・`決勝`・`SF`・`QF`・`R` を含むもの）を除外して算出します。

### 時刻変換
- **`etToJst(dateStr, timeStr)`** が ET → JST 変換を行います。**ET = UTC−4**
  固定オフセット（米国の夏時間。2026年6〜7月では正しい）を前提に、
  **JST 用に +9時間**します。`{date, time, weekday}` を返します。
  - ⚠️ この `+4` 固定オフセットは大会期間がすべて EDT 内だから正しいだけです。
    夏季以外の日付にこのヘルパーを再利用しないでください。

### ステージ設定
- **`STAGE_ORDER`** — ステージの描画順。
- **`STAGE_LABELS`** — 表示名（例: `"R16"` → `"ラウンド・オブ・16"`）。
- **`STAGE_COLORS`** — ステージごとのアクセントカラー（ステージヘッダーで使用）。

  新しいステージ値を追加する場合は、`RAW` に加えてこの**3つのマップすべて**を
  更新してください。

### 状態管理と描画
- モジュールレベルの状態: **`currentTeam`**（`'all'` またはチーム名）と
  **`searchText`**。
- **`buildTeamChips()`** — 読み込み時に一度だけチップ行を生成。
- **`selectTeam(team)`** — アクティブなチームフィルターを設定し、検索をクリアして
  再描画。
- **`applyFilter()`** — 検索ボックスを読み取る。検索するとチームフィルターは
  `'all'` にリセットされます。
- **`getFiltered()`** — 現在のチーム + 検索フィルターを `MATCHES` に適用。
- **`matchCard(m, mini)`** — 1試合分のカード HTML 文字列を返す。どちらかが
  `"日本"` の場合は日本戦の強調を付与。
- **`renderList()`** — フィルター結果をステージごとにグループ化して `#list` に
  書き込む。該当なしのときは `#empty` を表示。
- **`showTeamModal(team)` / `closeModal()`** — `#modal` オーバーレイを制御。
- ファイル末尾の起動処理: `buildTeamChips(); renderList();`。

描画はフレームワークを使わず、`innerHTML` への単純な文字列連結で行います。
イベントハンドラはインラインの `onclick`/`oninput` 属性と、モーダル背景用の
リスナー1つで配線しています。

## 規約

- **単一ファイル:** 強い理由がない限り、すべてを `index.html` に保つこと。
  ドロップイン・ゼロ依存というシンプルさがこのプロジェクトの肝です。
- **言語:** UI テキスト、（あれば）コメント、チーム/会場データはすべて日本語。
  既存の言語に合わせてください。
- **日程の編集:** `RAW` のみを変更すること。オブジェクトの形を一貫させ、
  時刻は ET（`etDate`/`etTime`）で持たせて JST 変換が正しく保たれるようにします。
- **時刻は ET で保存し、JST で表示する。** `RAW` に JST を保存しないこと。
- **スタイリング:** `:root` で定義した CSS カスタムプロパティ（`--bg`・`--gold`・
  `--red` など）を使用し、iOS のセーフエリア（`env(safe-area-inset-*)`）に対応
  しています。新しいハードコードの色より既存の変数を優先してください。
- CSS は意図的にコンパクト（1行に複数宣言）です。編集時は周囲のスタイルに
  合わせてください。

## Git ワークフロー

- 今回の作業ブランチ: **`claude/claude-md-docs-flhvrc`**。
- デフォルトブランチ: `main`。
- 明確で説明的なメッセージでコミットし、指定ブランチにプッシュすること。
- 明示的に依頼されない限り、プルリクエストは作成しないこと。
